# Propagate @Qualifier annotations to Lombok-generated constructor parameters
# so Spring can resolve qualified beans via constructor injection
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
